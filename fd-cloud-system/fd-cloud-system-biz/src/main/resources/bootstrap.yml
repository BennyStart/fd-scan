server:
  port: 8201
spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      discovery:
        server-addr: fd-cloud-nacos:8848
        namespace: tz
      config:
        file-extension: yml
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        shared-dataids: application-fd-cloud.yml
        namespace: ${spring.cloud.nacos.discovery.namespace}
feign:
  sentinel:
    enabled: true
resttemplate:
  sentinel:
    enabled: true
xxl:
  job:
    executor:
      #定时器-执行器端口号
      appname: fd-cloud-job-system
      port: 8210
