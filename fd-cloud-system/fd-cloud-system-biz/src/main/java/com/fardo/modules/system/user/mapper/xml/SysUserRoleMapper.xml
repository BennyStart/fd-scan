<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fardo.modules.system.user.mapper.SysUserRoleMapper">
    <select id="getRoleInfoByUserIds"  resultType="com.fardo.modules.system.user.vo.SysUserRoleVo">
		SELECT ur.*,r.role_name
		FROM t_sys_user_role ur inner join t_sys_role r on ur.role_id = r.id
		WHERE ur.user_id in
		<foreach collection="userIds" item="item" index="index"  open="(" separator="," close=")">
			#{item}
		</foreach>
    </select>
</mapper>